% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_load_liv.R
\name{db_load_liv}
\alias{db_load_liv}
\title{Load LIV data from the Singapore 2026 database table}
\usage{
db_load_liv(fc_ids = c("26FC00933"), make_unique = FALSE)
}
\arguments{
\item{fc_ids}{Character vector of FC IDs to load. Defaults to
\code{c("26FC00933")}.}

\item{make_unique}{Logical scalar. If \code{TRUE}, keeps a single representative
record set per file and per \code{fc_id}/channel/temperature combination. If
\code{FALSE}, returns all matching records. Defaults to \code{FALSE}.}
}
\value{
A data frame containing LIV measurements with columns:
\code{file_name}, \code{upload_time}, \code{file_created_time}, \code{work_order},
\code{measurement_step}, \code{int_cell_id}, \code{int_bar_id}, \code{int_die_id}, \code{fc_id},
\code{ch}, \code{temperature}, \code{current}, \code{power}, \code{voltage}, and \code{pd_current}.
}
\description{
Retrieves LIV measurements from the \code{LIV_SG_2026} database table for one or
more FC IDs. The function standardizes column names, converts current from
mA to A and power from mW to W, and returns the selected records as a local
data frame.
}
\details{
When \code{make_unique = TRUE}, duplicate uploads are reduced in two steps:
first, only the earliest upload is kept for each unique \code{file_name}; second,
only the latest upload is kept for each \code{fc_id}, channel, and temperature
combination.

The function opens a database connection using \code{connect_to_database()} and
closes it on exit with \code{DBI::dbDisconnect()}. Data manipulation is performed
using \code{dplyr} before the filtered result is collected into R.
}
\examples{
\dontrun{
db_load_liv()

db_load_liv(fc_ids = c("26FC00933", "26FC00934"))

db_load_liv(fc_ids = "26FC00933", make_unique = TRUE)
}
}
